#include "main.h"

/*
* _islower - to print a to z in lower case
* @c - the letters 
* Return: always 0
*/

int _isupper(int c)
{
	for (c = 'a'; c <= 'z'; c++)
	_putchar(c);
	_putchar('\n');

	return (0);
}
